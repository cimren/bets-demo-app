# Bets Demo App

A single-page betting bulletin application. The page displays a virtualized odds table and a sticky coupon at the bottom.

## Quick start

```bash
npm install
npm start        # dev server -> http://localhost:3000
npm run build    # production bundle -> dist/
npm test         # watch mode
npm run test:run # single run (CI)
```
